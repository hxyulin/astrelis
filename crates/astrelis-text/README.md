# astrelis-text

Backend-independent system/application font discovery, Unicode shaping,
line-breaking, bidi-aware caret and selection geometry, and immutable retained
text layouts.

Parley is an implementation detail; public APIs expose only Astrelis types.
